# Phase 0 구간 B — macOS 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Windows에서 만든 SPM 패키지 3종을 macOS에서 되살리고, Xcode 앱 골격을 세우고, 스파이크 S1·S2·S3의 남은 실측을 끝내 Phase 0 게이트를 닫는다.

**Architecture:** 기존 `docs/plans/2026-08-10-01-phase0-setup-spikes.md`의 Task 6~9를 대체한다. 그 문서를 쓴 뒤에 세 가지가 바뀌었기 때문이다 — ① 패키지가 2개에서 3개로 늘었고, ② `AppFont`가 앱 타깃이 아니라 `SoozipLayout`으로 갔고, ③ **패키지 2종이 macOS에서 아예 빌드되지 않는다는 사실이 2026-08-10 실측으로 드러났다.** 빌드 복구를 먼저 하고(Task 1~2), 문서를 실제 상태에 맞춘 뒤(Task 3), Xcode 골격을 세우고(Task 4~5), 실기기 스파이크를 돌린다(Task 6~7).

**Tech Stack:** Swift 6.3.3 / SwiftUI / SwiftData + CloudKit / Swift Testing / PencilKit / iOS 17+ / Xcode 26.6

## Global Constraints

`docs/plans/2026-08-10-00-roadmap.md`의 「전역 제약」 표 전체가 모든 태스크에 적용된다. 값을 그대로 옮긴다.

| 항목 | 값 |
|---|---|
| 최소 지원 | iOS 17.0 |
| 빌드 | Xcode 26 / iOS 26 SDK |
| 화면 방향 | Portrait · LandscapeLeft · LandscapeRight (Upside Down 제외) |
| 다크모드 | 미지원 — `UIUserInterfaceStyle = Light` 고정 |
| 기기 | iPhone 우선, iPad는 진행에 따라 판단 |
| 논리좌표 | 폭 **1080 고정**, 높이 **1350(4:5)** 또는 **1920(9:16)** |
| 레이어 상한 | `photo` 8 / `drawing` 5 / `text`+`shape`+`stamp` 합 30 |
| 입력 상한 | 모음집 이름 1~20자 · 캔버스 제목 0~40자 · 텍스트 레이어 200자 |
| 파스텔 12색 | `hsl(H, 55%, 82%)`, H = 0·30·60·90·120·150·180·210·240·270·300·330 |
| 캔버스 배경 | 흰색 `#FFFFFF` 고정 (P0) |
| CloudKit | 전 속성 기본값 또는 optional · `@Attribute(.unique)` 불가 · 관계는 양방향 optional · 첫 심사 후 additive-only |
| 외부 SDK | P0에서 0개 |
| 번들 ID | `com.<팀>.soozip` (소문자) / Product Name·모듈명은 `Soozip` |
| 표기 | 사용자에게 보이는 앱 이름은 전부 소문자 `soozip` |

**작업 브랜치:** 모든 태스크는 `feat/phase0-macos`에서 커밋한다. main 직접 커밋을 하지 않는다.

```bash
cd ~/projects/soozip && git checkout -b feat/phase0-macos
```

---

## 사전 조건 — 태스크별로 필요한 것

| 태스크 | 필요한 것 | 없으면 |
|---|---|---|
| 1~3 | macOS + Swift 6.3.3 (**확보됨**) | — |
| 4~5 | Xcode 26.6 (**확보됨**) | — |
| 6 (S1) | **실기기 1대** + Instruments | 시뮬레이터 측정은 무의미하므로 보류하고 게이트를 열어둔다 |
| 7 (S2) | **Apple Developer 계정** + 같은 iCloud 계정의 **실기기 2대** | Phase 1·2 진입 불가 — 아래 「S2가 막혔을 때」 참조 |

**S2가 막혔을 때.** S2는 "초안 폴더가 CloudKit에 동기화되지 않는다"를 확인하는데, 이건 **Phase 2 설계 전체가 딛고 선 전제**다(v4 §6.2). 추정으로 넘어가면 Phase 2를 다 만든 뒤에 뒤집힐 수 있다. 기기가 2대 없으면 **Task 7을 건너뛰지 말고 미완으로 남긴 채 Phase 1(데이터 레이어)까지만 진행**한다. Phase 2 승격 트랜잭션 착수 전에는 반드시 닫는다.

---

## File Structure

| 파일 | 책임 | 상태 |
|---|---|---|
| `Packages/SoozipGeometry/Package.swift` | 플랫폼 최소 버전 선언 | **수정** (Task 1) |
| `Packages/SoozipLayout/Package.swift` | 〃 (의존 사슬 정합) + 근거 주석 정정 | **수정** (Task 1) |
| `Packages/SoozipDraft/Package.swift` | 〃 | **수정** (Task 1) |
| `Packages/SoozipDraft/Sources/SoozipDraft/DraftStore.swift` | FileManager 보유 방식 | **수정** (Task 2) |
| `context/*/status.md` (6개) | 도메인별 구현 추적 | **수정** (Task 3) |
| `README.md` · `context/README.md` | 프로젝트 현재 상태 | **수정** (Task 3) |
| `Soozip.xcodeproj` | 앱 프로젝트 | **생성** (Task 4) |
| `Soozip/App/SoozipApp.swift` | 앱 진입점 | **생성** (Task 4) |
| `Soozip/Core/Geometry/CGInterop.swift` | `Vec2 ↔ CGPoint` 변환 — 패키지와 SwiftUI를 잇는 유일한 지점 | **생성 · 영구 자산** (Task 4) |
| `SoozipTests/CGInteropTests.swift` | 위의 단위 테스트 | **생성** (Task 4) |
| `Soozip/Resources/Fonts/*` | 폰트 5종 (이미 저장소에 있음) | 타깃에 편입 (Task 5) |
| `SoozipTests/FontLoadingTests.swift` | 폰트 5종 로드 · 글리프 확인 | **생성** (Task 5) |
| `Soozip/Spikes/S1_GestureProbe.swift` | 선택 UI 프로토타입 | 생성 → **Task 8에서 폐기** |
| `Soozip/Spikes/S2_CloudKitProbe.swift` | SwiftData 모델 프로브 | 생성 → **Task 8에서 폐기** |
| `docs/reports/2026-08-10-spike-results.md` | 실측 기록 | **수정** (Task 1·3·6·7) |

**`Soozip/Core/Layout/AppFont.swift`는 만들지 않는다.** `AppFont`는 `layoutJSON`의 `font` 값이라 Codec의 일부이고 Foundation만 쓰므로 이미 `Packages/SoozipLayout/Sources/SoozipLayout/AppFont.swift`에 있다. 앱 타깃에 중복 정의하면 두 개가 갈라진다.

---

### Task 1: SoozipGeometry의 macOS 빌드 복구

**문제.** `Package.swift`에 `platforms:`가 없어 macOS 기본 배포 타깃이 10.13이 된다. `SnapBench/main.swift:59`의 `Duration.components`는 macOS 13+이므로 컴파일에 실패하고, `swift test`가 실행 타깃까지 빌드하기 때문에 **26개 테스트가 멀쩡한데도 테스트 명령 전체가 실패**한다.

**근거 정정.** 세 패키지의 `Package.swift` 주석이 모두 "Windows에서 빌드되어야 하므로 platforms를 지정하지 않는다"고 적고 있는데 **이 전제가 틀렸다.** SPM의 `platforms:`는 Apple 플랫폼의 최소 버전만 제약하며 Windows·Linux 빌드에는 아무 영향이 없다. 이 잘못된 근거가 남아 있으면 같은 함정이 반복되므로 세 파일의 주석을 함께 고친다.

**Files:**
- Modify: `Packages/SoozipGeometry/Package.swift`
- Modify: `Packages/SoozipLayout/Package.swift` (주석만)
- Modify: `Packages/SoozipDraft/Package.swift` (주석만)

**Interfaces:**
- Consumes: 없음
- Produces: macOS에서 `swift test`가 성공하는 `SoozipGeometry`. Task 4의 Xcode 통합이 이 패키지를 로컬 의존성으로 붙인다.

- [ ] **Step 1: 실패를 먼저 확인한다**

```bash
cd ~/projects/soozip/Packages/SoozipGeometry && swift test 2>&1 | tail -5
```

Expected: `error: 'components' is only available in macOS 13.0 or newer` 로 빌드 실패.

- [ ] **Step 2: `platforms:` 선언 추가**

`Packages/SoozipGeometry/Package.swift` 전체를 아래로 교체한다.

```swift
// swift-tools-version: 6.0
import PackageDescription

// platforms는 **Apple 플랫폼의 최소 버전만** 제약한다. Windows·Linux 빌드에는
// 영향이 없으므로 이 선언이 있어도 `swift test`는 Windows에서 그대로 돈다.
//
// macOS 13이 필요한 이유: SnapBench가 쓰는 Duration.components가 macOS 13+다.
// 선언이 없으면 기본 타깃이 10.13이 되어 `swift test`가 SnapBench 컴파일에서
// 죽는다 — 라이브러리와 테스트는 멀쩡한데 명령 전체가 실패한다.
let package = Package(
    name: "SoozipGeometry",
    platforms: [.iOS(.v17), .macOS(.v13)],
    products: [
        .library(name: "SoozipGeometry", targets: ["SoozipGeometry"])
    ],
    targets: [
        .target(name: "SoozipGeometry"),
        .testTarget(
            name: "SoozipGeometryTests",
            dependencies: ["SoozipGeometry"]
        ),
        // 벤치마크는 테스트가 아니다(통과/실패가 아니라 수치를 낸다).
        // 반드시 `swift run -c release SnapBench`로 돌린다 — debug 빌드는
        // 수십 배 느려서 60fps 예산 판단에 쓸 수 없다.
        .executableTarget(
            name: "SnapBench",
            dependencies: ["SoozipGeometry"]
        )
    ]
)
```

- [ ] **Step 3: 테스트 통과 확인**

```bash
cd ~/projects/soozip/Packages/SoozipGeometry && swift test 2>&1 | grep -E "error:|Test run with"
```

Expected: `✔ Test run with 26 tests in 0 suites passed`

- [ ] **Step 4: 나머지 두 패키지에도 같은 선언을 넣는다**

> **실행 중 정정 (2026-08-10).** 이 계획은 원래 "SoozipLayout·SoozipDraft는 오래된 API만 쓰므로 선언이 필요 없고 주석만 고치면 된다"고 적고 있었다. **틀렸다.** SoozipGeometry에만 넣고 돌리니 SPM이 거부한다.
>
> ```
> error: the library 'SoozipLayout' requires macos 10.13, but depends on the
> product 'SoozipGeometry' which requires macos 13.0
> ```
>
> `platforms:`는 **의존 사슬 전체가 같은 값을 들어야 한다.** SPM은 의존 대상보다 낮은 최소 버전을 허용하지 않는다. 세 패키지 모두 선언한다.

`Packages/SoozipLayout/Package.swift`:

```swift
// platforms는 Apple 플랫폼의 최소 버전만 제약한다 — Windows·Linux 빌드와 무관하다.
//
// 이 패키지 자체는 Foundation의 오래된 API만 쓰지만 선언이 필요하다.
// SPM은 **의존 대상보다 낮은 최소 버전을 허용하지 않는다.** SoozipGeometry가
// macOS 13을 요구하므로(SnapBench의 Duration.components) 이쪽도 맞춰야 한다.
let package = Package(
    name: "SoozipLayout",
    platforms: [.iOS(.v17), .macOS(.v13)],
    products: [
```

`Packages/SoozipDraft/Package.swift`:

```swift
// platforms를 선언하는 이유는 SoozipLayout과 같다 — 의존 사슬의 최소 버전을 맞춘다.
let package = Package(
    name: "SoozipDraft",
    platforms: [.iOS(.v17), .macOS(.v13)],
    products: [
```

- [ ] **Step 5: 세 패키지 전부 다시 돌린다**

```bash
cd ~/projects/soozip/Packages/SoozipGeometry && swift test 2>&1 | grep "Test run with"
cd ~/projects/soozip/Packages/SoozipLayout   && swift test 2>&1 | grep "Test run with"
```

Expected: 각각 26개, 40개 통과. (`SoozipDraft`는 Task 2에서 고친다 — 지금은 여전히 실패한다.)

- [ ] **Step 6: 커밋**

```bash
cd ~/projects/soozip
git add Packages/SoozipGeometry/Package.swift Packages/SoozipLayout/Package.swift Packages/SoozipDraft/Package.swift
git commit -m "fix: SoozipGeometry에 platforms 선언 — macOS에서 SnapBench 빌드 실패 해소"
```

---

### Task 2: DraftStore의 플랫폼 의존 누수 제거

**문제.** `DraftStore`는 `Sendable`을 선언하면서 `FileManager`를 저장 프로퍼티로 들고 있다. `FileManager`는 **swift-corelibs-foundation(Windows)에서는 Sendable이지만 Darwin Foundation에서는 아니다.** 그래서 Windows에서 24개가 통과한 패키지가 macOS에서는 컴파일조차 되지 않는다. 로드맵 Phase 0 게이트가 *"결과가 다르면 플랫폼 의존이 새어 들어간 것"* 이라고 예고한 바로 그 사례다.

**해법 선택.** 두 안을 실측했고 둘 다 24개를 통과시킨다.

| 안 | 방법 | 판정 |
|---|---|---|
| A | 주입 파라미터를 없애고 `FileManager.default`를 계산 프로퍼티로 | **채택** — 경고 0건, 억누르는 검사 없음 |
| B | `nonisolated(unsafe) private let fm: FileManager` | 기각 — 통과하지만 Sendable 검사를 억누른다 |

**A를 고르는 근거는 주입이 실제로 쓰이지 않는다는 것이다.** `DraftStoreTests` 24개 전부 `DraftStore(root:)`만 호출하고 커스텀 `FileManager`를 넘기는 곳이 하나도 없다. 아무도 안 쓰는 확장점 때문에 플랫폼 의존을 떠안는 것은 거래가 맞지 않는다. 나중에 정말 필요해지면 그때 프로토콜로 추상화한다.

**Files:**
- Modify: `Packages/SoozipDraft/Sources/SoozipDraft/DraftStore.swift:60-72`

**Interfaces:**
- Consumes: Task 1의 `SoozipLayout`
- Produces:
  - `public init(root: URL)` — **`fileManager:` 파라미터가 사라진다.** 이 이니셜라이저를 부르는 곳은 아직 테스트뿐이며, Phase 2의 승격 트랜잭션이 앱에서 이 시그니처로 호출한다.
  - 나머지 공개 API(`create` · `load` · `all` · `draft(forCollection:)` · `writeLayout` · `readLayout` · `writePhoto` · `readPhoto` · `photoFormat` · `photoIDs` · `delete` · `pruneOrphans`)는 그대로다.

- [ ] **Step 1: 실패를 먼저 확인한다**

```bash
cd ~/projects/soozip/Packages/SoozipDraft && swift build 2>&1 | grep "error:" | head -3
```

Expected: `error: stored property 'fm' of 'Sendable'-conforming struct 'DraftStore' has non-Sendable type 'FileManager'`

- [ ] **Step 2: 저장 프로퍼티를 계산 프로퍼티로 바꾼다**

`Packages/SoozipDraft/Sources/SoozipDraft/DraftStore.swift`에서 아래 블록을 찾는다.

```swift
    public let root: URL
    private let fm: FileManager

    private static let metaFile = "meta.json"
    private static let layoutFile = "layout.json"
    private static let photosDir = "photos"

    public init(root: URL, fileManager: FileManager = .default) {
        self.root = root
        self.fm = fileManager
    }
```

아래로 교체한다.

```swift
    public let root: URL

    private static let metaFile = "meta.json"
    private static let layoutFile = "layout.json"
    private static let photosDir = "photos"

    public init(root: URL) {
        self.root = root
    }

    /// `FileManager`를 저장 프로퍼티로 들지 않는 이유:
    /// corelibs-foundation(Windows)에서는 `Sendable`이지만 **Darwin에서는 아니다.**
    /// 들고 있으면 이 구조체의 `Sendable` 선언이 macOS에서만 깨져,
    /// Windows에서 통과한 테스트가 Mac에서 컴파일조차 되지 않는다.
    ///
    /// 주입 파라미터를 없앤 것은 아무도 쓰지 않았기 때문이다. 테스트 24개 전부
    /// 임시 디렉터리 경로만 넘긴다. 정말 필요해지면 그때 프로토콜로 뺀다.
    /// `FileManager.default`는 여러 스레드에서 호출해도 안전하다고 문서화돼 있다.
    private var fm: FileManager { .default }
```

- [ ] **Step 3: 테스트 통과 확인**

```bash
cd ~/projects/soozip/Packages/SoozipDraft && swift test 2>&1 | grep -E "error:|warning:|Test run with"
```

Expected: `✔ Test run with 24 tests in 0 suites passed`, 에러·경고 0건

- [ ] **Step 4: 세 패키지 전부 통과하는지 확인 — 게이트 항목이다**

```bash
cd ~/projects/soozip
for p in SoozipGeometry SoozipLayout SoozipDraft; do
  printf '%-16s ' "$p"
  (cd Packages/$p && swift test 2>&1 | grep -o "Test run with [0-9]* tests.*passed" || echo FAILED)
done
```

Expected:
```
SoozipGeometry   Test run with 26 tests in 0 suites passed
SoozipLayout     Test run with 40 tests in 0 suites passed
SoozipDraft      Test run with 24 tests in 0 suites passed
```

합계 **90개**. 이 수치가 로드맵 Phase 0 게이트의 "macOS에서도 통과한다" 항목을 닫는다.

- [ ] **Step 5: 커밋**

```bash
git add Packages/SoozipDraft/Sources/SoozipDraft/DraftStore.swift
git commit -m "fix: DraftStore의 FileManager 보유 제거 — Darwin에서 Sendable 위반"
```

---

### Task 3: 문서를 실제 상태에 맞춘다

**문제.** `README.md`와 `context/README.md`가 아직 **"코드 0줄 — 설계 완료 단계"** 라고 적고 있다. 실제로는 소스 902줄에 테스트 90개다. `context/*/status.md` 6개 파일도 전부 ⬜(미반영)인데, 그중 여럿은 이미 구현이 끝났다. 프로젝트 원칙이 *"코드가 문서를 앞서가지 않는다"* 인데 지금 상태가 그 위반이다.

**이 태스크를 Xcode 작업보다 먼저 두는 이유.** 이후 태스크가 `status.md`를 갱신하며 진행되는데, 출발점이 틀려 있으면 무엇이 새로 채워진 것인지 구분되지 않는다.

**Files:**
- Modify: `README.md:8-10`
- Modify: `context/README.md:51-53`
- Modify: `context/editor/status.md`
- Modify: `context/canvas/status.md`
- Modify: `context/sync/status.md`
- Modify: `docs/reports/2026-08-10-spike-results.md`

**Interfaces:**
- Consumes: Task 1·2의 통과 결과
- Produces: 없음 (문서 전용)

- [ ] **Step 1: 루트 README의 현재 상태 절 교체**

`README.md`의 「현재 상태」 절을 아래로 바꾼다.

```markdown
## 현재 상태

**Phase 0 진행 중.** 기하·레이아웃·초안 저장소 3개 패키지가 완성됐고 (테스트 90개, macOS·Windows 양쪽 통과), Xcode 앱 골격과 실기기 스파이크가 남았습니다.

| 패키지 | 내용 | 테스트 |
|---|---|---|
| `SoozipGeometry` | 좌표 변환 · 리사이즈 · 스냅 후보 계산 | 26 |
| `SoozipLayout` | layoutJSON v1 Codec · 폰트 5종 · 레이어 상한 | 40 |
| `SoozipDraft` | 초안 파일 I/O · 고아 정리 | 24 |
```

- [ ] **Step 2: context/README의 상태 절 교체**

`context/README.md`의 「프로젝트 상태」 절을 아래로 바꾼다.

```markdown
## 프로젝트 상태 (2026-08-10)

**Phase 0 진행 중.** `Packages/` 아래 3개 패키지(테스트 90개)가 완성됐습니다. 남은 것은 Xcode 앱 골격, 실기기 스파이크 S1(제스처·60fps)·S2(CloudKit 동기화)입니다. 상세는 `docs/plans/2026-08-10-02-phase0-macos.md`.
```

- [ ] **Step 3: editor/status.md의 완료 항목 표시**

`context/editor/status.md`에서 아래 4개 행의 상태를 `⬜` → `✅`로 바꾸고 커밋 해시를 채운다.

| 항목 | 바꿀 행 |
|---|---|
| 레이어 JSON v1 인코딩/디코딩 | `\| ✅ \| 레이어 JSON v1 인코딩/디코딩 \| §8 \| c790168 \|` |
| 폰트 5종 번들 (OFL) | `\| ✅ \| 폰트 5종 번들 + 스와치 (OFL) \| §5.5 \| 4c655f9 — 스와치 UI는 Phase 5 \|` |
| 폰트 서브셋 | `\| ✅ \| 폰트 서브셋 (KS X 1001 2,350자) \| §5.5, S4 \| 4c655f9 — 혼합 5.2MB \|` |
| 레이어 상한 | `\| ✅ \| 레이어 상한 (photo 8 / drawing 5 / 나머지 30) \| §5.13 \| c790168 — 검증 로직만, 도구 비활성화는 Phase 5 \|` |

**부분 완료는 부분이라고 적는다.** 폰트 스와치 UI와 도구 비활성화는 아직 없으므로 비고에 남긴다. 다 됐다고 적으면 Phase 5에서 빠뜨린다.

- [ ] **Step 4: canvas/status.md와 sync/status.md의 완료 항목 표시**

`context/canvas/status.md`:

```markdown
| ✅ | 초안 저장소 — `Drafts/` 파일 I/O | §6.2 | ed938fa |
| ✅ | 사진 사본 즉시 기록 (디바운스 제외) | §6.5 | ed938fa — `writePhoto` |
| ✅ | 모음집당 초안 1개 제한 + "이어서 / 버리고 새로" | §6.5 | ed938fa — 조회만, UI는 Phase 6 |
| ✅ | 고아 초안 정리 (앱 시작 시 스캔) | §6.9 | ed938fa — `pruneOrphans`, 배선은 Phase 2 |
```

`context/sync/status.md`:

```markdown
| ✅ | `Drafts/` 파일 I/O (읽기·쓰기·삭제) | §6.2 | ed938fa |
| ✅ | 고아 초안 정리 (앱 시작 시 스캔) | §6.9 | ed938fa — 배선은 Phase 2 |
```

- [ ] **Step 5: 스파이크 보고서에 macOS 실측 추가**

`docs/reports/2026-08-10-spike-results.md`의 「게이트 진행 상황」 절 바로 앞에 아래를 넣는다.

```markdown
## macOS 크로스체크 (2026-08-10)

Windows에서 통과한 패키지를 macOS(Apple Silicon, Swift 6.3.3 / Xcode 26.6)에서 돌린 결과다.

| 패키지 | Windows | macOS 첫 시도 | 원인 |
|---|---|---|---|
| SoozipGeometry | 26 통과 | **빌드 실패** | `platforms:` 미선언 → 기본 타깃 10.13. SnapBench의 `Duration.components`가 macOS 13+ |
| SoozipLayout | 40 통과 | 40 통과 | — |
| SoozipDraft | 24 통과 | **빌드 실패** | `DraftStore: Sendable`이 `FileManager` 보유. corelibs-foundation에서는 Sendable이지만 **Darwin에서는 아니다** |

**로드맵 게이트가 예고한 "플랫폼 의존 누수"가 실제로 두 건 발생했다.** 순수 Swift + Foundation만 쓴다고 해서 플랫폼 독립이 보장되지 않는다는 것이 이 실측의 교훈이다 — 같은 `Foundation`이라도 **Darwin과 corelibs는 Sendable 적합성이 다르다.** 수정 후 90개 전부 양쪽에서 통과한다.

### SnapBench 재측정 — Apple Silicon

| 레이어 수 | Windows x86_64 | macOS arm64 | 프레임 예산 점유율(arm64) |
|---|---|---|---|
| 10 (전부 축정렬) | 4.20 µs | 1.64 µs | 0.010% |
| 20 (전부 축정렬) | 6.97 µs | 1.40 µs | 0.008% |
| **42 (설계 상한)** | 14.64 µs | **2.20 µs** | **0.013%** |
| 42 (절반 회전) | 7.77 µs | 1.42 µs | 0.009% |
| 42 (전부 회전) | 0.16 µs | 0.06 µs | 0.000% |
| 100 (상한 2배) | 27.56 µs | 5.17 µs | 0.031% |

**arm64가 6.7배 빠르다.** S1-a에 적어둔 한계 문구("iPhone의 ARM CPU는 이보다 느리다")는 x86_64 데스크톱과 비교한 보수적 가정이었는데, M 계열에서는 반대로 나왔다. iPhone의 A 계열은 M 계열보다 느리므로 이 수치가 곧 iPhone 값은 아니지만, **"계산은 병목이 아니다"라는 결론의 여유는 오히려 더 커졌다.**

20개(1.40 µs)가 10개(1.64 µs)보다 빠르게 나온 것은 측정 노이즈다. 두 값 모두 프레임 예산의 0.01% 수준이라 구분에 의미가 없다.
```

- [ ] **Step 6: 게이트 진행 상황 갱신**

같은 파일의 「구간 B (macOS)」 체크리스트에서 첫 줄을 아래로 바꾼다.

```markdown
- [x] macOS에서 같은 26개 통과 (+ SoozipLayout 40 · SoozipDraft 24 = **90개**) — 플랫폼 의존 누수 2건 수정 후
```

- [ ] **Step 7: 커밋**

```bash
git add README.md context/ docs/reports/
git commit -m "docs: 문서를 실제 구현 상태에 동기화 + macOS 크로스체크 실측 기록"
```

---

### Task 4: Xcode 프로젝트 생성과 패키지 3종 통합

**Files:**
- Create: `Soozip.xcodeproj`
- Create: `Soozip/App/SoozipApp.swift`
- Create: `Soozip/Core/Geometry/CGInterop.swift`
- Test: `SoozipTests/CGInteropTests.swift`

**Interfaces:**
- Consumes: Task 1·2가 고친 패키지 3종
- Produces:
  - 앱 타깃 `Soozip`, 테스트 타깃 `SoozipTests`
  - `extension CGPoint { init(_ v: Vec2) }` · `extension Vec2 { init(_ p: CGPoint) }`
  - `extension CGSize { init(_ s: Size2) }` · `extension Size2 { init(_ s: CGSize) }`
  - Phase 3의 SwiftUI 뷰가 이 변환으로 기하 로직과 통신한다. **패키지와 UI가 만나는 유일한 지점**이라 변환 비용도 여기에만 있다.

> **이 태스크는 Xcode GUI 조작을 포함한다.** 프로젝트 생성만은 자동화하지 않는다 — `project.pbxproj`를 손으로 쓰는 것보다 GUI가 정확하고, XcodeGen을 들이면 "외부 의존성 0개" 원칙에 개발 도구가 하나 붙는다.

- [ ] **Step 1: Xcode에서 프로젝트 생성**

Xcode → File → New → Project → iOS → App

| 항목 | 값 |
|---|---|
| Product Name | `Soozip` |
| Organization Identifier | `com.<팀식별자>` |
| Bundle Identifier | 생성 후 **`com.<팀식별자>.soozip`(소문자)** 로 직접 수정 |
| Interface | SwiftUI |
| Language | Swift |
| Testing System | **Swift Testing with XCTest UI Tests** |
| Storage | **None** (SwiftData는 Task 7 스파이크와 Phase 1에서 직접 구성) |
| 저장 위치 | `~/projects/soozip` (저장소 루트) |

**Bundle ID만 소문자다.** v4 §1이 표시 표기는 `soozip`, 코드 식별자·모듈명은 `Soozip`, 번들 ID는 `com.<팀>.soozip`으로 갈라 놨다.

- [ ] **Step 2: 전역 제약을 프로젝트 설정에 적용**

프로젝트 → TARGETS → Soozip → General:

| 설정 | 값 |
|---|---|
| Minimum Deployments | **iOS 17.0** |
| Supported Destinations | iPhone만 (iPad 체크 해제 — 미정) |
| iPhone Orientation | Portrait · Landscape Left · Landscape Right (**Upside Down 해제**) |

TARGETS → Soozip → Info 탭에서 키 2개 추가:

| Key | Type | Value |
|---|---|---|
| `UIUserInterfaceStyle` | String | `Light` |
| `ITSAppUsesNonExemptEncryption` | Boolean | `NO` |

- [ ] **Step 3: 로컬 패키지 3종 추가**

1. Xcode → File → **Add Package Dependencies** → **Add Local...**
2. `~/projects/soozip/Packages/SoozipGeometry` 선택
3. 같은 방법으로 `SoozipLayout`, `SoozipDraft` 추가
4. TARGETS → Soozip → General → Frameworks, Libraries, and Embedded Content에 **3개 모두** 추가

**원래 계획서는 2개만 붙이라고 적혀 있는데 그건 `SoozipDraft`가 생기기 전에 쓰인 것이다.** 의존 방향은 `SoozipDraft → SoozipLayout → SoozipGeometry`라 추가 순서는 상관없다.

- [ ] **Step 4: 폴더 구조 생성**

```bash
cd ~/projects/soozip/Soozip
mkdir -p App Core/{Layout,Image,Rendering,Geometry,Haptics} \
         Data/{Models,DraftStore,Repository} \
         Features/{CollectionHome,CollectionDetail,Viewer,Search,Profile} \
         Features/Editor/{Canvas,Selection,SmartGuide,Tools} \
         Spikes
```

Xcode에서 각 디렉터리를 프로젝트에 추가한다(드래그 시 **"Create groups"**). 템플릿이 만든 `SoozipApp.swift`와 `ContentView.swift`를 `App/`으로 옮긴다. `Resources/Fonts/`는 이미 저장소에 있으므로 Task 5에서 편입한다.

- [ ] **Step 5: 실패하는 테스트 작성**

`SoozipTests/CGInteropTests.swift`:

```swift
import Testing
import CoreGraphics
import SoozipGeometry
@testable import Soozip

@Test func Vec2와_CGPoint는_상호변환된다() {
    let v = Vec2(x: 123.5, y: 456.75)
    let p = CGPoint(v)
    #expect(p.x == 123.5)
    #expect(p.y == 456.75)
    #expect(Vec2(p) == v)
}

@Test func Size2와_CGSize는_상호변환된다() {
    let s = Size2(width: 1080, height: 1350)
    let cg = CGSize(s)
    #expect(cg.width == 1080)
    #expect(cg.height == 1350)
    #expect(Size2(cg) == s)
}

@Test func 논리좌표_왕복변환은_값을_잃지_않는다() {
    // 논리좌표는 Double이고 CGFloat도 64비트 플랫폼에서 Double이다.
    // 왕복에서 값이 변하면 레이어가 저장할 때마다 조금씩 밀린다.
    let original = Vec2(x: 1079.9999, y: 1349.0001)
    #expect(Vec2(CGPoint(original)) == original)
}
```

- [ ] **Step 6: 테스트 실패 확인**

```bash
cd ~/projects/soozip
xcodebuild test -scheme Soozip -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | tail -20
```

Expected: 컴파일 실패 — `cannot find 'CGPoint(_:)' initializer`

> 시뮬레이터 이름이 다르면 `xcrun simctl list devices available | grep iPhone` 으로 확인해 바꾼다.

- [ ] **Step 7: 최소 구현**

`Soozip/Core/Geometry/CGInterop.swift`:

```swift
import CoreGraphics
import SoozipGeometry

// SoozipGeometry는 CoreGraphics에 의존하지 않는다 — Windows에서도 빌드되어야 하고,
// 기하 로직이 UI에서 물리적으로 분리되는 효과가 덤으로 따라온다.
// SwiftUI와 잇는 지점이 이 파일 하나뿐이므로 변환 비용도 여기에만 있다.

public extension CGPoint {
    init(_ v: Vec2) { self.init(x: v.x, y: v.y) }
}

public extension Vec2 {
    init(_ p: CGPoint) { self.init(x: Double(p.x), y: Double(p.y)) }
}

public extension CGSize {
    init(_ s: Size2) { self.init(width: s.width, height: s.height) }
}

public extension Size2 {
    init(_ s: CGSize) { self.init(width: Double(s.width), height: Double(s.height)) }
}
```

- [ ] **Step 8: 테스트 통과 확인**

```bash
xcodebuild test -scheme Soozip -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | grep -E "Test run with|TEST (SUCCEEDED|FAILED)"
```

Expected: `TEST SUCCEEDED`, 3개 테스트 통과

- [ ] **Step 9: 커밋**

```bash
git add -A
git commit -m "chore: Xcode 프로젝트 생성 + 패키지 3종 통합 + CGInterop"
```

---

### Task 5: 폰트 번들 등록과 로드 검증 (S3 마무리)

**남은 것만 한다.** 폰트 5종 수집·라이선스 확인·서브셋·용량 측정(**5.2MB**)·PostScript 이름 추출은 2026-08-10에 Windows에서 끝났고 `AppFont`도 `SoozipLayout`에 구현돼 있다. Mac에 남은 건 **번들 등록과 실제 iOS 로드 확인**뿐이다.

**Files:**
- Modify: `Soozip.xcodeproj` (Fonts를 Copy Bundle Resources에 편입 + `UIAppFonts` 등록)
- Test: `SoozipTests/FontLoadingTests.swift`

**Interfaces:**
- Consumes: `SoozipLayout`의 `AppFont` — `postScriptName` · `displayName` · `coversFullHangul` · `allCases`
- Produces: 검증된 폰트 번들. Phase 5의 텍스트 도구가 `UIFont(name: font.postScriptName, size:)`로 쓴다.

- [ ] **Step 1: 폰트 파일을 타깃에 편입**

1. Finder에서 `~/projects/soozip/Soozip/Resources/Fonts`를 Xcode 프로젝트 네비게이터로 드래그
2. **"Create folder references"가 아니라 "Create groups"** 선택
3. Target Membership에서 **Soozip 체크**
4. TARGETS → Soozip → Build Phases → **Copy Bundle Resources**에 `.ttf` 4개와 `.otf` 1개가 들어 있는지 확인

**여기서 가장 흔한 실패가 Copy Bundle Resources 누락이다.** `Info.plist`에 등록해도 파일이 번들에 안 들어가면 `UIFont(name:)`이 조용히 `nil`을 준다. `licenses/` 폴더는 리소스로 넣지 않아도 된다 — Phase 9에서 설정 화면에 실을 때 다시 다룬다.

- [ ] **Step 2: `UIAppFonts` 등록**

TARGETS → Soozip → Info 탭 → 키 추가 → **"Fonts provided by application"** (`UIAppFonts`), Type은 Array, 항목 5개:

```
Pretendard-Regular.otf
GowunBatang-Regular.ttf
GowunDodum-Regular.ttf
NanumPenScript-Regular.ttf
PlayfairDisplay-Regular.ttf
```

`Info.plist` 파일이 실제로 생성됐다면 아래와 같은 형태다.

```xml
<key>UIAppFonts</key>
<array>
    <string>Pretendard-Regular.otf</string>
    <string>GowunBatang-Regular.ttf</string>
    <string>GowunDodum-Regular.ttf</string>
    <string>NanumPenScript-Regular.ttf</string>
    <string>PlayfairDisplay-Regular.ttf</string>
</array>
```

**여기 적는 것은 파일명이고, `AppFont.postScriptName`이 주는 것은 PostScript 이름이다.** 나눔손글씨만 둘이 다르다 — 파일은 `NanumPenScript-Regular.ttf`, PostScript 이름은 `NanumPen-Regular`.

> **선행 검증 (2026-08-10, 저장소의 실제 폰트 파일에서 name 테이블을 직접 파싱).** 5종 전부 `AppFont.postScriptName`과 일치한다. 나눔손글씨의 패밀리명은 `Nanum Pen`이라 `"NanumPenScript-Regular"`는 패밀리명으로도 안 잡힌다 — Step 3의 부정 단언이 성립하는 근거다.
>
> | 파일 | family | postscript |
> |---|---|---|
> | GowunBatang-Regular.ttf | Gowun Batang | `GowunBatang-Regular` |
> | GowunDodum-Regular.ttf | Gowun Dodum | `GowunDodum-Regular` |
> | NanumPenScript-Regular.ttf | **Nanum Pen** | **`NanumPen-Regular`** |
> | PlayfairDisplay-Regular.ttf | Playfair Display | `PlayfairDisplay-Regular` |
> | Pretendard-Regular.otf | Pretendard | `Pretendard-Regular` |

- [ ] **Step 3: 실패하는 테스트 작성**

`SoozipTests/FontLoadingTests.swift`:

```swift
import Testing
import UIKit
import SoozipLayout

// AppFont는 앱 타깃이 아니라 SoozipLayout에 있다.
// layoutJSON의 `font` 값이라 Codec의 일부이기 때문이다.

@Test func 번들_폰트_5종이_모두_로드된다() {
    for font in AppFont.allCases {
        let loaded = UIFont(name: font.postScriptName, size: 20)
        #expect(loaded != nil, "폰트 로드 실패: \(font.postScriptName)")
    }
}

@Test func 나눔손글씨는_파일명이_아니라_PostScript이름으로_로드된다() {
    // 파일은 NanumPenScript-Regular.ttf인데 PostScript 이름은 NanumPen-Regular다.
    // 파일명으로 부르면 nil이 온다 — 이 함정을 테스트로 고정해 둔다.
    #expect(UIFont(name: "NanumPen-Regular", size: 20) != nil)
    #expect(UIFont(name: "NanumPenScript-Regular", size: 20) == nil)
}

@Test func 일상_한글_글리프가_누락되지_않았다() {
    // 감성 3종은 KS X 1001 2,350자로 서브셋했다. 일상 음절은 전부 들어 있어야 한다.
    let sample = "모음집 안녕하세요 가나다라마바사 2026년 8월"
    for font in [AppFont.gowunBatang, .gowunDodum, .nanumPen] {
        let uiFont = UIFont(name: font.postScriptName, size: 20)!
        let attributed = NSAttributedString(string: sample, attributes: [.font: uiFont])
        #expect(attributed.size().width > 0, "\(font.displayName) 렌더 실패")
    }
}

@Test func 서브셋_폰트의_희귀음절은_시스템폰트로_폴백한다() {
    // `뷁`은 KS X 1001에 없다. 글자가 사라지지 않고 폰트만 섞여 보이는 것이
    // 의도한 동작이다(v4 §5.5). 폭이 0이면 글자가 통째로 증발한 것이다.
    let rare = "뷁힣똠"
    let uiFont = UIFont(name: AppFont.gowunBatang.postScriptName, size: 20)!
    let attributed = NSAttributedString(string: rare, attributes: [.font: uiFont])
    #expect(attributed.size().width > 0)
}

@Test func Pretendard는_전체_한글을_담는다고_선언한다() {
    #expect(AppFont.pretendard.coversFullHangul)
    #expect(!AppFont.gowunBatang.coversFullHangul)
}

@Test func 알수없는_폰트이름은_nil을_반환한다() {
    #expect(UIFont(name: "NonExistentFont-Regular", size: 20) == nil)
}
```

- [ ] **Step 4: 테스트 실행**

```bash
xcodebuild test -scheme Soozip -destination 'platform=iOS Simulator,name=iPhone 17' \
  -only-testing:SoozipTests/FontLoadingTests 2>&1 | grep -E "Test run with|failed|TEST"
```

Expected: 6개 통과.

실패하면 순서대로 확인한다.
1. `번들_폰트_5종이_모두_로드된다`가 깨졌다 → Copy Bundle Resources 또는 `UIAppFonts` 누락
2. 특정 폰트만 깨졌다 → PostScript 이름 불일치. 실제 값은 아래로 확인한다

```bash
python3 -c "
from fontTools.ttLib import TTFont
import glob
for f in sorted(glob.glob('Soozip/Resources/Fonts/*.[to]tf')):
    n = TTFont(f, fontNumber=0)['name'].getDebugName(6)
    print(f.split('/')[-1], '->', n)
"
```

(`fontTools`가 없으면 `pip install fonttools`)

- [ ] **Step 5: 보고서의 S3-f 항목 닫기**

`docs/reports/2026-08-10-spike-results.md`의 「S3-f. Mac에 남은 것」을 아래로 바꾼다.

```markdown
### S3-f. Mac 확인 완료 (2026-08-10)

- [x] `Info.plist`의 `UIAppFonts` 등록
- [x] `UIFont(name:)`로 5종 실제 로드 확인 — `SoozipTests/FontLoadingTests` 6개 통과
- [x] 서브셋 후 한글 글리프 누락 없음 — 일상 음절 렌더 확인, 희귀 음절(`뷁힣똠`)은 의도대로 시스템 폰트 폴백

**S3 종료.** 폰트 5종 5.2MB, 기준(10MB)의 52%.
```

- [ ] **Step 6: 커밋**

```bash
git add -A
git commit -m "feat: 폰트 5종 번들 등록 + iOS 로드 검증 — S3 종료"
```

---

### Task 6: S1 스파이크 — 선택 UI 프로토타입과 실기기 60fps 실측

**사전 조건: 실기기 1대.** 시뮬레이터는 Mac의 GPU를 쓰므로 프레임 수치가 무의미하다.

**이 태스크의 코드는 전부 버린다.** 남는 것은 숫자뿐이다.

**Files:**
- Create: `Soozip/Spikes/S1_GestureProbe.swift`
- Modify: `Soozip/App/SoozipApp.swift`
- Modify: `docs/reports/2026-08-10-spike-results.md`

**Interfaces:**
- Consumes: `SoozipGeometry`(`LayerFrame` · `Corner` · `snapCandidates`) + Task 4의 `CGInterop`
- Produces: 실측 수치. **코드는 Task 8에서 삭제한다.**

- [ ] **Step 1: 프로토타입 화면 작성**

`Soozip/Spikes/S1_GestureProbe.swift`:

```swift
import SwiftUI
import SoozipGeometry

/// S1 스파이크 전용. Phase 3에서 정식 구현으로 대체하고 이 파일은 삭제한다.
/// 목적은 "예쁘게 만들기"가 아니라 **레이어 43개에서 60fps가 나오는지**만 보는 것이다.
struct S1_GestureProbe: View {
    @State private var frames: [LayerFrame] = (0..<43).map { i in
        LayerFrame(center: Vec2(x: 120 + Double(i % 7) * 140,
                                y: 150 + Double(i / 7) * 180),
                   size: Size2(width: 100, height: 100),
                   rotation: 0)
    }
    @State private var selected: Int? = nil
    @State private var zoom: CGFloat = 1.0
    @State private var pan: CGSize = .zero
    @State private var activeSnaps: [SnapCandidate] = []

    private let canvasSize = Size2(width: 1080, height: 1350)

    var body: some View {
        GeometryReader { geo in
            let fit = min(geo.size.width / canvasSize.width,
                          geo.size.height / canvasSize.height)
            let scale = fit * zoom

            ZStack {
                Color.white

                ForEach(frames.indices, id: \.self) { i in
                    Rectangle()
                        .fill(i == selected ? Color.pink.opacity(0.5)
                                            : Color.gray.opacity(0.3))
                        .frame(width: frames[i].size.width * scale,
                               height: frames[i].size.height * scale)
                        .rotationEffect(.radians(frames[i].rotation))
                        .position(x: frames[i].center.x * scale + pan.width,
                                  y: frames[i].center.y * scale + pan.height)
                        .onTapGesture { selected = i }
                }

                ForEach(activeSnaps.indices, id: \.self) { i in
                    let snap = activeSnaps[i]
                    Rectangle()
                        .fill(Color(red: 1, green: 0, blue: 1))   // SwiftUI에 Color.magenta는 없다
                        .frame(width: snap.axis == .vertical ? 1 : geo.size.width,
                               height: snap.axis == .vertical ? geo.size.height : 1)
                        .position(x: snap.axis == .vertical
                                     ? snap.value * scale + pan.width
                                     : geo.size.width / 2,
                                  y: snap.axis == .vertical
                                     ? geo.size.height / 2
                                     : snap.value * scale + pan.height)
                }

                if let s = selected {
                    SelectionOverlayProbe(frame: frames[s], scale: scale, pan: pan)
                }
            }
            .gesture(dragGesture(scale: scale))
            .gesture(magnifyGesture())
        }
    }

    private func dragGesture(scale: CGFloat) -> some Gesture {
        DragGesture()
            .onChanged { value in
                guard let s = selected else {
                    pan = value.translation
                    return
                }
                frames[s].center = Vec2(x: (value.location.x - pan.width) / scale,
                                        y: (value.location.y - pan.height) / scale)

                let others = frames.enumerated()
                    .filter { $0.offset != s }
                    .map(\.element)
                // 임계는 화면 좌표 8pt다. 논리좌표로 계산하면 줌 배율에 따라 감각이 달라진다.
                activeSnaps = snapCandidates(for: frames[s],
                                             among: others,
                                             canvasSize: canvasSize,
                                             threshold: 8 / Double(scale))
            }
            .onEnded { _ in activeSnaps = [] }
    }

    private func magnifyGesture() -> some Gesture {
        MagnifyGesture()
            .onChanged { value in
                guard selected == nil else { return }   // 선택 있으면 레이어 담당
                zoom = min(max(value.magnification, 0.5), 4.0)
            }
    }
}

private struct SelectionOverlayProbe: View {
    let frame: LayerFrame
    let scale: CGFloat
    let pan: CGSize

    var body: some View {
        ZStack {
            ForEach(Corner.allCases, id: \.self) { corner in
                let p = frame.corner(corner)
                Rectangle()
                    .fill(Color.white)
                    .frame(width: 12, height: 12)          // 시각 12pt
                    .overlay(Rectangle().stroke(Color.blue, lineWidth: 1))
                    .frame(width: 44, height: 44)          // 히트 영역은 44pt
                    .contentShape(Rectangle())
                    .position(x: p.x * scale + pan.width,
                              y: p.y * scale + pan.height)
            }
        }
    }
}
```

- [ ] **Step 2: 임시 루트로 연결**

`Soozip/App/SoozipApp.swift`:

```swift
import SwiftUI

@main
struct SoozipApp: App {
    var body: some Scene {
        WindowGroup {
            S1_GestureProbe()   // Phase 0 전용. Task 8에서 제거한다.
        }
    }
}
```

- [ ] **Step 3: 실기기에 설치하고 4개 기준을 측정**

| 측정 | 방법 | 통과 기준 |
|---|---|---|
| 60fps | Instruments → **Animation Hooks**, 레이어 43개 상태로 10초 연속 드래그 | 평균 60fps, 드롭 프레임 5% 미만 |
| 회전 리사이즈 | 레이어 하나를 45° 회전시킨 뒤 코너 드래그 | 대각 반대편이 눈에 띄게 움직이지 않음 |
| 핸들 히트 | 줌 200%에서 핸들 가장자리를 탭 | 12pt 그래픽 밖 44pt 영역에서도 잡힘 |
| 제스처 배타 | 선택 있는 상태 / 없는 상태에서 각각 핀치 | 레이어 / 캔버스로 정확히 갈림 |

**Animation Hooks와 SwiftUI View Body를 함께 켠다.** 계산이 아니라 body 재평가가 병목임을 확인하기 위해서다.

- [ ] **Step 4: 측정값을 보고서에 기록**

`docs/reports/2026-08-10-spike-results.md`의 S1 절 표에서 보류 3개 행을 실측값으로 채우고, 아래 형식으로 S1-b를 추가한다. **"잘 동작함" 같은 서술을 쓰지 않는다 — 숫자만 적는다.**

```markdown
### S1-b. 실기기 렌더링 측정 (YYYY-MM-DD)

| 항목 | 값 |
|---|---|
| 기기 / iOS | |
| 평균 fps (레이어 43, 10초 드래그) | |
| 드롭 프레임 비율 | |
| 최대 body 재평가 횟수 / 프레임 | |
| 회전 45° 코너 드래그 시 고정점 이탈 | ○ px |
| 줌 200%에서 핸들 히트 성공률 | / 10회 |
| 제스처 배타 (선택 유/무 핀치) | |
```

- [ ] **Step 5: 60fps 미달 시 대응 결정**

> **선행 실측:** 스냅 **계산** 비용은 42개 레이어에서 Windows 14.64 µs / macOS arm64 2.20 µs, 프레임 예산의 **0.088% / 0.013%** 다. **계산 축 대응책(스로틀링, 후보 축소)은 이미 폐기됐다** — 0.1% 미만인 것을 줄여 얻을 게 없다.

미달이면 **렌더링 축**에서 아래 순서로 시도하고, 결정된 것을 v4 설계서 §5.8.4에 반영한다.

1. 가이드선을 매 프레임 새 뷰로 만들지 않고 identity를 유지해 재사용
2. 오버레이에 `drawingGroup()` 적용 — Metal 오프스크린 합성
3. 드래그 중 갱신 범위를 선택 레이어 + 가이드선으로 한정 (전체 캔버스 재구성 방지)
4. 위 셋으로 안 되면 균등 간격 가이드를 P1로 이월 (표시 요소가 가장 많다)

- [ ] **Step 6: `context/editor/status.md`의 60fps 항목 갱신**

측정이 끝났으면 `| ⬜ | 60fps 유지 검증 | §5.8.4, S1 | |` 행을 결과와 함께 `✅` 또는 대응책 메모로 바꾼다.

- [ ] **Step 7: 커밋**

```bash
git add -A
git commit -m "spike: S1 제스처·선택 UI·스마트 가이드 실기기 실측"
```

---

### Task 7: S2 스파이크 — SwiftData + CloudKit 실기기 2대 동기화

**사전 조건: Apple Developer 계정 + 같은 iCloud 계정으로 로그인한 실기기 2대.**

**이 스파이크가 Phase 2 설계 전체의 전제를 검증한다.** "초안 폴더가 CloudKit에 동기화되지 않는다"가 틀리면 v4 §6.2가 통째로 무너지고 초안을 SwiftData에 넣는 설계로 되돌아가야 한다.

**Files:**
- Create: `Soozip/Spikes/S2_CloudKitProbe.swift`
- Modify: `Soozip/App/SoozipApp.swift`
- Modify: `docs/reports/2026-08-10-spike-results.md`

**Interfaces:**
- Consumes: 없음
- Produces: 검증된 모델 제약. **Phase 1에서 `Data/Models/` 아래 정식 모델을 다시 쓰되 여기서 확인한 제약을 그대로 적용한다.**

- [ ] **Step 1: CloudKit 컨테이너 설정**

Xcode → TARGETS → Soozip → Signing & Capabilities:

1. Team을 본인 Apple Developer 팀으로 설정
2. **+ Capability → iCloud** 추가
3. Services에서 **CloudKit** 체크
4. Containers에서 **+** 로 `iCloud.com.<팀식별자>.soozip` 생성
5. **+ Capability → Background Modes** 추가 후 **Remote notifications** 체크

- [ ] **Step 2: 프로브 모델 작성**

`Soozip/Spikes/S2_CloudKitProbe.swift`:

```swift
import SwiftUI
import SwiftData

// S2 스파이크 전용. Phase 1에서 Data/Models/ 아래 정식 모델로 대체하고 삭제한다.
// CloudKit 제약을 그대로 지킨다: 전 속성 기본값 또는 optional,
// @Attribute(.unique) 불가, 관계는 양방향 optional.

@Model final class ProbeCollection {
    var id: UUID = UUID()
    var name: String = ""
    var createdAt: Date = Date()
    var sortIndex: Int = 0
    var coverCanvasID: String = ""
    var canvases: [ProbeCanvas]? = []
}

@Model final class ProbeCanvas {
    var id: UUID = UUID()
    var aspect: Int = 0
    var title: String = ""
    var createdAt: Date = Date()
    var updatedAt: Date = Date()
    var layoutJSON: Data = Data()
    @Attribute(.externalStorage) var renderedPNG: Data?
    var collection: ProbeCollection?
}

struct S2_CloudKitProbe: View {
    @Environment(\.modelContext) private var context
    @Query(sort: \ProbeCollection.createdAt) private var collections: [ProbeCollection]
    @State private var draftFolderExists = false

    var body: some View {
        NavigationStack {
            List {
                Section("모음집 \(collections.count)개") {
                    ForEach(collections) { c in
                        VStack(alignment: .leading) {
                            Text(c.name)
                            Text("캔버스 \(c.canvases?.count ?? 0)개")
                                .font(.caption).foregroundStyle(.secondary)
                        }
                    }
                }
                Section("초안 폴더") {
                    Text(draftFolderExists ? "존재함" : "없음")
                    Button("초안 폴더 만들기") { makeDraftFolder() }
                    Button("다시 확인") { checkDraftFolder() }
                }
            }
            .toolbar { Button("추가") { addSample() } }
            .onAppear { checkDraftFolder() }
        }
    }

    private func addSample() {
        let c = ProbeCollection()
        c.name = "테스트 \(Int.random(in: 100...999))"
        let canvas = ProbeCanvas()
        canvas.title = "캔버스"
        canvas.collection = c
        context.insert(c)
        context.insert(canvas)
        try? context.save()
    }

    private var draftsURL: URL {
        URL.applicationSupportDirectory.appending(path: "Drafts")
    }

    private func makeDraftFolder() {
        try? FileManager.default.createDirectory(at: draftsURL,
                                                 withIntermediateDirectories: true)
        try? "probe".data(using: .utf8)?
            .write(to: draftsURL.appending(path: "probe.txt"))
        checkDraftFolder()
    }

    private func checkDraftFolder() {
        draftFolderExists = FileManager.default
            .fileExists(atPath: draftsURL.appending(path: "probe.txt").path())
    }
}
```

- [ ] **Step 3: 앱에 모델 컨테이너 연결**

`Soozip/App/SoozipApp.swift`:

```swift
import SwiftUI
import SwiftData

@main
struct SoozipApp: App {
    var body: some Scene {
        WindowGroup {
            S2_CloudKitProbe()   // Phase 0 전용. Task 8에서 제거한다.
        }
        .modelContainer(for: [ProbeCollection.self, ProbeCanvas.self])
    }
}
```

- [ ] **Step 4: 실기기 2대로 검증**

| 검증 | 절차 | 통과 기준 |
|---|---|---|
| 양방향 동기화 | A에서 [추가] → B에서 확인, B에서 [추가] → A에서 확인 | 30초 내 반영 |
| 관계 동기화 | A에서 캔버스 추가 → B에서 개수 확인 | 개수 일치 |
| **초안 미동기화** | A에서 [초안 폴더 만들기] → 60초 뒤 B에서 [다시 확인] | **B가 "없음"** |
| 로컬 모드 | 기기 하나를 iCloud 로그아웃 → 앱 사용 | 크래시 없이 로컬 동작 |

- [ ] **Step 5: 결과 기록**

`docs/reports/2026-08-10-spike-results.md`의 S2 절을 아래로 교체하고 값을 채운다.

```markdown
## S2 — SwiftData + CloudKit

✅ **측정 완료 (YYYY-MM-DD)**

| 항목 | 결과 |
|---|---|
| 기기 A / B | |
| 양방향 동기화 반영 시간 | A→B ○초 / B→A ○초 |
| 관계(`Collection ↔ Canvas`) 동기화 | |
| **초안 폴더 동기화 여부** | **동기화되지 않음 / 됨** |
| iCloud 로그아웃 시 동작 | |

**판정:** 초안 미동기화가 확인되어 v4 §6.2(초안을 SwiftData에 넣지 않는다)의 전제가 성립한다.
```

**초안이 동기화되는 것으로 나오면 여기서 멈춘다.** Phase 2 설계를 다시 열어야 하므로 v4 §6.2를 수정하고 로드맵 Phase 2를 재작성한 뒤에 진행한다.

- [ ] **Step 6: `context/sync/status.md` 갱신**

`실기기 2대 동기화 검증`과 `**초안이 동기화되지 않는지** 검증` 두 행을 `✅`로 바꾼다.

- [ ] **Step 7: 커밋**

```bash
git add -A
git commit -m "spike: S2 SwiftData+CloudKit 실기기 2대 동기화 검증"
```

---

### Task 8: 스파이크 폐기와 Phase 0 게이트 정산

**Files:**
- Delete: `Soozip/Spikes/` 전체
- Modify: `Soozip/App/SoozipApp.swift`
- Modify: `docs/reports/2026-08-10-spike-results.md`
- Modify: `docs/plans/2026-08-10-00-roadmap.md`

**Interfaces:**
- Consumes: Task 6·7의 실측 결과
- Produces: Phase 1이 딛고 설 깨끗한 앱 골격

- [ ] **Step 1: 스파이크 코드 삭제**

```bash
cd ~/projects/soozip
rm -rf Soozip/Spikes
```

Xcode에서 `Spikes` 그룹 참조도 제거한다.

**`Packages/`와 `Soozip/Core/Geometry/CGInterop.swift`는 남긴다.** 검증된 자산이고 Phase 3~4가 그 위에 세워진다.

- [ ] **Step 2: 앱 진입점을 빈 상태로 되돌린다**

`Soozip/App/SoozipApp.swift`:

```swift
import SwiftUI

@main
struct SoozipApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Phase 1 대기 중")
                .foregroundStyle(.secondary)
        }
    }
}
```

`.modelContainer(for:)`도 함께 지운다 — 정식 모델은 Phase 1에서 `Data/Models/`에 새로 쓴다.

- [ ] **Step 3: 전체 테스트 실행**

```bash
cd ~/projects/soozip
for p in SoozipGeometry SoozipLayout SoozipDraft; do
  printf '%-16s ' "$p"
  (cd Packages/$p && swift test 2>&1 | grep -o "Test run with [0-9]* tests.*passed" || echo FAILED)
done
xcodebuild test -scheme Soozip -destination 'platform=iOS Simulator,name=iPhone 17' 2>&1 | grep -E "Test run with|TEST (SUCCEEDED|FAILED)"
```

Expected:
- 패키지 **90개** 통과 (26 + 40 + 24)
- 앱 **9개** 통과 (CGInterop 3 + Font 6)

> 로드맵 게이트에는 "앱 테스트 6개"라고 적혀 있는데 그건 `AppFont`가 앱 타깃에 있을 때의 계산이다. 폰트 테스트를 6개로 늘렸으므로 9개가 맞다. Step 5에서 로드맵을 함께 고친다.

- [ ] **Step 4: 스파이크 보고서의 게이트 절 마감**

`docs/reports/2026-08-10-spike-results.md`의 「구간 B (macOS)」 체크리스트를 실제 결과로 전부 채운다. 미달 항목이 있으면 **체크하지 말고 왜 못 닫았는지 한 줄로 적는다.** 통과하지 않은 것을 통과했다고 적으면 Phase 1에서 그 대가를 치른다.

- [ ] **Step 5: 로드맵의 Phase 0 게이트 갱신**

`docs/plans/2026-08-10-00-roadmap.md`의 Phase 0 게이트 체크박스를 결과로 채우고, 마지막 「다음 행동」 절을 아래로 바꾼다.

```markdown
## 다음 행동

**Phase 0 완료.** 상세 실행 기록은 `docs/plans/2026-08-10-02-phase0-macos.md`, 실측은 `docs/reports/2026-08-10-spike-results.md`.

**Phase 1 상세 계획:** `docs/plans/2026-08-10-03-phase1-data-layer.md` (스파이크 결과를 반영해 작성한다)
```

- [ ] **Step 6: 커밋과 푸시**

```bash
git add -A
git commit -m "chore: 스파이크 코드 폐기 + Phase 0 게이트 정산"
git push -u origin feat/phase0-macos
```

---

## Phase 0 게이트 — 이 계획이 닫아야 하는 것

Phase 1로 넘어가기 전에 전부 확인한다.

**구간 A (완료됨)**
- [x] `swift test`가 Windows에서 성공하고 26개가 통과한다
- [x] `SoozipGeometry`에 CoreGraphics·SwiftUI import가 하나도 없다

**구간 B**
- [ ] 세 패키지가 macOS에서 **90개** 통과한다 (Task 1·2)
- [ ] `xcodebuild test`가 성공하고 앱 테스트 **9개**가 통과한다 (Task 4·5)
- [ ] 보고서에 S1·S2·S3의 **실측 수치**가 기록되어 있다 (숫자로, 서술이 아니라)
- [ ] S1의 60fps 결과가 기록되고, 미달이면 대응책이 v4 §5.8.4에 반영되었다 (Task 6)
- [ ] S2에서 **초안 폴더가 동기화되지 않음**이 실기기 2대로 확인되었다 (Task 7)
- [ ] S3에서 폰트 5종 합계 용량이 기록되었다 — **5.2MB, 기준의 52%** (Task 5)
- [ ] `Soozip/Spikes/`가 삭제되었고 `Packages/`와 `CGInterop.swift`는 남아 있다 (Task 8)
- [ ] 전역 제약(iOS 17 / 방향 3종 / Light 고정)이 프로젝트 설정에 적용되었다 (Task 4)
- [ ] 문서가 실제 상태와 어긋나지 않는다 (Task 3·8)
- [ ] 모든 변경이 커밋되고 원격에 푸시되었다

## 다음

Phase 0 게이트 통과 후 `docs/plans/2026-08-10-03-phase1-data-layer.md`를 작성한다. **스파이크 결과를 반영해서 쓴다** — 미리 쓰지 않는 이유가 그것이다.
