# editor 구현 추적

> v4 설계서의 P0 요구사항별 구현 상태를 추적합니다.
>
> **최종 갱신: 2026-08-22 · EDITOR-10까지 반영.**
> 이 도메인의 🟡은 거의 전부 **"기하·상태 로직은 `SoozipGeometry`/`SoozipLayout`에
> 있고 SwiftUI 화면이 없다"**는 뜻이다. 에디터 화면은 아직 한 줄도 없다
> (`Presentation/Views/`에는 모음집 관련 4개뿐).

## 범례

- ✅ 반영됨 — 코드에 구현 완료
- 🟡 부분 반영 — 아래 계층은 됐고 위 계층이 남음 (예: 데이터 레이어 완료 · UI 미구현)
- ⬜ 미반영 — 정책/설계만 확정, 코드 미구현

## P0 — 기반 (스파이크 S1 대상)

| 상태 | 항목 | 설계 참조 | PR/커밋 |
|------|------|-----------|---------|
| 🟡 | 레이어 공통 조작 경로 (선택·이동·핀치·회전·투명도) | §5.6 | ca9155f · 57ab01f(#12) — `LayerStore`가 5종을 한 경로로 담고 `LayerTransform`이 회전·투명도를 든다. **선택 상태 완료**(`selection`/`select`/`deselect`, `entries` 파생이라 삭제·재정렬에 자동 추종). **이동·핀치 제스처는 `EDITOR-10`·`11`** |
| ✅ | 역회전 행렬 (회전 레이어 드래그·리사이즈) | §5.6, S1 | `LayerFrame.toLocal`/`toWorld` — 회전 0°·45°·90°에서 검증 |
| 🟡 | 바운딩 박스 + 코너 4 핸들 | §5.7 | 57ab01f(#12) — `HandlePlacement.Box`가 코너 4점을 **화면 좌표로** 낸다(회전 반영). **박스 실선 렌더는 `EDITOR-11`** |
| 🟡 | 변 핸들 (shape·text 전용) | §5.7 | 57ab01f(#12) — **타입 제한 완료**: `LayerKind.resizableEdges`가 photo·stamp·drawing에 빈 집합, text는 좌우, shape는 네 변. ⚠️ **종류 축 문지기일 뿐** — 크기 축(88/56pt)은 `EDITOR-6`, 실제 드래그는 `EDITOR-7` |
| 🟡 | 회전 핸들 (상단 40pt 이내 시 아래로 뒤집기) | §5.7 | 57ab01f(#12) — 배치·뒤집기 판정 완료. **기준선은 캔버스 상단이 아니라 뷰포트 상단**(결정 4, v4 §5.7 각주 참조) + `up.y ≤ 0`일 때만 뒤집는다(BR-6). **렌더는 `EDITOR-11`** |
| 🟡 | 삭제 ✕ 핸들 + 스낵바 실행취소 | §5.7 | 57ab01f(#12) · f4e6778(#13) — 배치 완료(**좌상단 코너와 동일 지점**). 승자는 **제스처가 후보를 거르고 `orderedHandles` 순서가 고른다** — 탭이 삭제를 잡는 것은 순서 때문이고 드래그가 좌상단을 잡는 것만이 제스처 때문이다(`EDITOR-5`). **스낵바 실행취소는 미구현** |
| 🟡 | 핸들 히트 영역 44pt / 시각 12pt | §5.7 | f4e6778(#13) — **히트 판정 완료**: 화면 축 정렬 44×44pt 정사각형(원 아님), 경계 22pt 포함, 줌 50~400% 무관. 줌 무관은 구조가 아니라 네 테스트가 고정한다(`Box`가 코너를 `public let`으로 공개해 회전·배율 복원이 가능하다). **시각 12pt 렌더는 `EDITOR-11`** |
| ✅ | 핸들 겹침 방지 (88pt / 56pt 단계) | §5.7 | `EDITOR-6` — 짧은 변이 88pt 미만이면 **변 핸들 전부**를 숨기고(축별 부분 숨김이 아니다), 56pt 미만이면 코너를 화면 축 방향으로 22pt씩 민다. **두 경계가 반대 관례다** — `>= 88`이라 정확히 88이면 변이 있고, `< 56`이라 정확히 56이면 안 민다(둘 다 "경계는 이전 상태"). 밀 때 `Corner.sign`을 화면 델타로 쓰면 회전 범위 절반에서 안쪽으로 밀린다 |
| ✅ | 크기 하한 40px · 상한 캔버스 긴 변 ×4 | §5.7 | `EDITOR-7` — **`LayerFrame.resizeLimits(canvas:)`가 단일 출처.** `Size2` 하나를 받아 하한 40(캔버스 무관)과 상한(긴 변×4 → 4:5는 5400, 9:16은 7680)을 함께 낸다. `CanvasAspect`는 `SoozipLayout`에 있어 참조 불가라 `Size2`를 받는다. **하한이 상한을 이긴다**(클램프가 상한 먼저·하한 나중) — 비율 192(9:16)·135(4:5)를 넘는 가는 레이어에서 짧은 변이 새던 것을 막는다. 배선은 `EDITOR-11`이며 **호출부는 반드시 이 함수를 거쳐야 한다**(타입이 강제하지 않는다) |
| ✅ | 코너 드래그 대각 고정점 (회전 0°·45°·90°) | §5.6, §5.7 | `EDITOR-7` — 90° 축이 새로 관측됐다. ⚠️ **"고정점이 유지된다"는 단언은 대수적 항등이라 어떤 변이도 죽이지 못한다** — 보정 블록이 중심 계산을 통째로 소거한다. 실질 관측면은 `size`와 끌린 코너다 |
| 🟡 | 캔버스 줌 50~400% + 더블탭 fit | §5.9 | a185285 — `CanvasSurface.zoomLimits`(0.5~4.0) · `fitted()`. **더블탭 제스처는 `EDITOR-11`** |
| 🟡 | 캔버스 팬 (작업 영역까지 + 고무줄) | §5.9 | a185285 — `workArea` · `clampedToWorkArea`. **고무줄 감쇠는 `EDITOR-11`** |
| ✅ | 제스처 배타 처리 + 우선순위 표 | §5.9 | `EDITOR-10` — `GestureRouter`가 `(FingerPattern 3종, hasSelection)` **6조합을 전수 switch**로 판정하고(`_` 와일드카드 금지 — 케이스 추가가 조용히 컴파일되지 않게), 확정된 라우팅을 종료까지 **잠근다**. 판정 표는 `private`이라 배선이 매 프레임 직접 부를 수 없다. ⚠️ **막는 것은 동시 시작이지 해제 후 즉시 재잠금이 아니다** — `ended()`를 누가 보내는가가 §13 재발 여부를 정하고 그 통제권은 `EDITOR-11`(`ExclusiveGesture`)에 있다. ⚠️ "레이어" 결과는 **대상 존재를 보장하지 않는다**(소비자가 조용히 무시할 것) |
| 🟡 | 캔버스 클리핑 + 고스트 렌더 (30%) | §5.10 | `EDITOR-9`(#17) — **기하 판정 완료.** `LayerFrame.overlap(canvas:)`가 `.inside`/`.partial`/`.outside` 3분류를 SAT 4축으로 낸다(회전 반영, AABB 근사 금지). **30%는 불투명도이지 판정 임계가 아니다** — 렌더는 `EDITOR-11`. ⚠️ `.partial`은 **양의 넓이를 함의하지 않는다**(한 점 접촉·부동소수 오차 포함) — 렌더 패스 트리거나 사용자 신호로 쓰면 안 된다 |
| 🟡 | 레이어 중심 작업 영역 제한 | §5.10 | `EDITOR-9`(#17) — **`CanvasSurface.clampedLayerCenter(_:)`가 판정을 낸다.** `workArea`를 재계산하지 않고 `clampedToWorkArea`를 그대로 부른다(팬 한계와 레이어 한계는 같은 사각형이라 두 벌로 갈라지면 조용히 어긋난다). 비유한 center는 **캔버스 중심으로 후퇴** — 방어 없이 부르면 `∞`가 `1620`으로 조용히 유한해지고 `NaN`은 통과해 `JSONEncoder`가 던진다. 그래서 `clampedToWorkArea`는 이제 `internal`이다. **`EDITOR-10`이 호출 강제 게이트를 세웠다** — `ClampedLayerCenter` 토큰(생성자가 하나뿐이고 그 안에서 클램프를 부른다) + 세 계층의 `placed`/`place` + 세터 셋을 `internal(set)`으로. `clampedLayerCenter`도 이제 `internal`이다. ⚠️ **닫힌 것은 5개 경로 중 이동 축 하나뿐이다** — 나머지 넷이 무엇이고 각각 왜 열려 있는지는 `architecture.md`의 「중심을 바꾸는 경로」 표가 **정본이다**(여기에 목록을 복제하지 않는다). `LayerStore.place`는 **봉쇄가 아니라 선점**이다 |
| ✅ | z-order 4종 + 0부터 재정렬 | §5.11 | ca9155f — `bringToFront`/`sendToBack`/`bringForward`/`sendBackward`, 조작 후 z가 0부터 촘촘히 |
| ⬜ | 가로 모드 툴바 재배치 | §1.2 | `EDITOR-11` |
| ✅ | 기기 회전 시 줌 배율 재계산 | §6.9 | a185285 — `viewportChanged(to:)`. `zoom`은 fit 대비 배수, `center`는 논리 지점이라 **보정 코드가 필요 없다** |

## P0 — 스마트 가이드

> **로드맵에서 v1.1로 이월했지만 계산 로직은 이미 있다** — Phase 0 성능 스파이크
> 산물(`SnapEngine`, 테스트 9). 남은 것은 배선과 햅틱이라, v1에 넣는 판단도 가능하다.

| 상태 | 항목 | 설계 참조 | PR/커밋 |
|------|------|-----------|---------|
| 🟡 | 정렬 가이드 (캔버스 중심선 + 레이어 6기준선) | §5.8.1 | `snapCandidates` — `.alignment`. **가이드선 렌더는 `EDITOR-11`** |
| 🟡 | 균등 간격 가이드 (3개 이상, 이중 화살표) | §5.8.1 | `snapCandidates` — `.equalSpacing`. **화살표 렌더 미구현** |
| 🟡 | 크기 일치 가이드 (리사이즈 중) | §5.8.1 | `snapCandidates` — `.sizeMatch` |
| ✅ | 회전 15° 스냅 (±3°) | §5.8.1 | `EDITOR-8`(#16) — **`RotationSnap`이 단일 출처.** `SnapKind`에 케이스를 넣지 않았다(가이드 선 렌더러가 소비자인데 회전엔 그을 선이 없고, `public` enum이라 케이스 추가가 소스 호환성 파괴다). **판정 본체는 도(degree) 공간** — 라디안에서 재면 `\|12°−15°\|`가 임계보다 작아져 "정확히 3°는 미흡착"이 깨진다. **경계는 이전 상태**(정확히 3°는 미흡착). 흡착은 정규화 이전 원본 기준이라 바퀴 수가 보존된다(373→375). 배선은 `EDITOR-11`이며 **호출부는 `gridStepDegrees`·`snapThresholdDegrees`를 읽어야 한다** |
| ✅ | 축 정렬 판정 — 한 바퀴 돈 레이어 (버그 수정) | §5.8.2 | `EDITOR-8`(#16) — `isAxisAligned`가 **가장 가까운 2π 배수까지의 거리**로 판정한다. 이전에는 `abs(rotation) < 0.0001`이라 한 바퀴 돈 레이어가 후보에서 빠졌다. ⚠️ **`[0,2π)` 정규화 후 0 근접 비교로 바꾸면 미세 음수 회전이 깨진다**(`-0.00005` → `6.28313…`). 허용오차 0.0001 rad는 회전 스냅의 3°와 **목적이 다른 별개 규칙**이다 |
| 🟡 | 스냅 임계 화면 좌표 8pt | §5.8.2 | `threshold`를 **화면 좌표로 주입받는다**(논리좌표면 줌에 따라 감각이 달라짐). 값 8pt는 호출부 몫 |
| ✅ | 회전 레이어를 후보에서 제외 | §5.8.2 | 움직이는 쪽·상대 쪽 모두 제외 — 회전체의 바운딩 박스는 눈에 안 맞아 보인다 |
| ⬜ | 햅틱 4종 구분 (light/medium/selection) | §5.8.3 | |
| 🟡 | 동시 다중 히트 시 햅틱 1회 · enter 이벤트만 | §5.8.2 | `EDITOR-8`(#16) — **회전 축의 enter 판정만 있다**(`RotationSnap.entersSnap` 도·라디안 2종). 현재가 흡착 중이고 (직전이 미흡착이거나 목표 그리드가 다르면) 참이며, 목표 비교를 정규화해 `359°→1°` 랩어라운드에서 재발화하지 않는다. ⚠️ **정렬·균등간격·크기일치는 여전히 상태가 없다** — `snapCandidates`는 매 프레임 후보만 내고 직전 프레임을 모른다. **햅틱 재생 코드는 저장소 전체에 0건**이라 '동시 다중 히트 시 1회'는 미착수(`EDITOR-11`) |
| 🟡 | 60fps 유지 검증 | §5.8.4, S1 | 6c64d4a — **계산 비용은 프레임 예산의 0.088%로 실측.** 미달하면 원인은 무조건 렌더링 축이다 |

## P0 — 도구

| 상태 | 항목 | 설계 참조 | PR/커밋 |
|------|------|-----------|---------|
| ⬜ | 도구 패널 7종 (배경은 비활성 슬롯) | §5.1 | `TOOL-4` |
| ⬜ | `text` 레이어 + 입력 시트 + 정렬 | §6.4 | `TOOL-1` — `TextLayer`(폰트·크기·색·정렬) 모델은 있다 |
| ✅ | 폰트 5종 번들 + 스와치 (OFL) | §5.5 | 4c655f9 — 파일·`AppFont` 완료, **스와치 UI는 `TOOL-1`** |
| ✅ | 폰트 서브셋 (KS X 1001 2,350자) | §5.5, S4 | 4c655f9 — 혼합안 5.2MB (Pretendard만 전체 한글) |
| ⬜ | `shape` 레이어 9종 | §5.3 | `TOOL-2` — `ShapeKind` 9종 모델은 있다 |
| ⬜ | 파스텔 12색 팔레트 + 마지막 색 기억 | §5.3, §5.4 | `TOOL-2` |
| ⬜ | `stamp` 날짜 도장 | §6.4 | **v1.1로 이월** |
| ⬜ | `drawing` PencilKit + 이미지 베이크 | §6.4 | **v1.1로 이월** |
| ✅ | 레이어 상한 (photo 8 / drawing 5 / 나머지 30) | §5.13 | 4a483ab — **`LayerCategory`가 분류·상한의 단일 출처.** `LayoutDocument.validate()`(사후)와 `LayerStore.insert()`(삽입 차단)가 둘 다 그것을 쓴다 |
| 🟡 | 상한 도달 시 도구 비활성화 + 안내 | §5.13 | 4a483ab — `canInsert`/`remaining`/`count` 판정 완료. **배선은 `EDITOR-11`이며 안내 문구는 반드시 `category.limit`을 읽어야 한다**(하드코딩하면 다시 두 벌) |
| ⬜ | 입력 상한 — 텍스트 200자 | §6.9 | `TOOL-1` |
| ⬜ | **레이어 복제 (전 타입, +40,+40, 복제본 선택)** | §5.12.1 | `TOOL-3` — 복제가 `insert`를 타므로 상한은 자동으로 걸린다(경로 미검증) |
| ⬜ | **상한 도달 시 [복제] 비활성화** | §6.9 | `TOOL-3` |
| ⬜ | **클립보드 붙여넣기 버튼 (`UIPasteControl`)** | §5.12.2 | `MEDIA-5` |
| ✅ | 레이어 JSON v1 인코딩/디코딩 | §8 | c790168 — `SoozipLayout`. **상한 초과 문서를 불러올 때 잘라내지 않는다**(작업물 증발 금지), 더 넣는 것만 막는다 |

## P1

| 상태 | 항목 | 설계 참조 |
|------|------|-----------|
| ⬜ | 레이어 그룹 (도형+텍스트 함께 이동) | §19 |
| ⬜ | 배경 종이 질감·단색 | §19 |
| ⬜ | 템플릿 프리셋 | §19 |
| ⬜ | 마스킹테이프·스티커 | §19 |
| ⬜ | 폰트 ODR 확장 | §19 |
