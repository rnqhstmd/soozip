# collection 구현 추적

> v4 설계서의 P0 요구사항별 구현 상태를 추적합니다.
>
> **최종 갱신: 2026-08-12 (PR #9 / EDITOR-3까지 반영).**
> Phase 6(모음집 화면, PR #3)에서 이 도메인의 화면이 전부 닫혔다 — 남은 ⬜는
> 다른 도메인(에디터·검색)에 딸린 것들이다.

## 범례

- ✅ 반영됨 — 코드에 구현 완료
- 🟡 부분 반영 — 아래 계층은 됐고 위 계층이 남음 (예: 데이터 레이어 완료 · UI 미구현)
- ⬜ 미반영 — 정책/설계만 확정, 코드 미구현

## P0

| 상태 | 항목 | 설계 참조 | PR/커밋 |
|------|------|-----------|---------|
| ✅ | `Collection` 모델 + SwiftData/CloudKit 스키마 | §7 | PR #1 — 제약 4종 자동 검증 |
| ✅ | 모음집 생성 (이름 입력 시트, 1~20자) | §4 | PR #3 — `CollectionEditSheet(mode: .create)`. **생성 직후 상세 진입은 에디터(Phase 3)와 함께** |
| ✅ | 모음집 이름 변경 | §4 | PR #3 — 카드 컨텍스트 메뉴 → `renameCollection` |
| ✅ | 모음집 삭제 — 빈 경우 즉시, 캔버스 N장이면 개수 명시 경고 후 cascade | §6.8 | PR #3 — `DeletionPrompt` + `confirmationDialog`. 상수 바인딩을 피해 모든 닫기 경로에서 탈출 가능 |
| ✅ | 가로 캐러셀 + 맨 좌측 [+] 고정 | §2.2 | PR #3 — `[+]` 위치를 `CollectionPresenter.carouselItems`가 정한다(뷰 재량 아님) |
| ✅ | 카드 표지 렌더 (대표 → 최근 → 단색 폴백) | §4 | PR #3 — `CoverArt` 3단계 + `CollectionCardView` |
| 🟡 | 대표 캔버스 지정 | §4 | PR #3 — `LibraryRepository.setCover` 존재. **호출부 없음** — 캔버스 컨텍스트 메뉴가 생기는 시점(`CANVAS-11` 뷰어)에 배선 |
| ✅ | 표지 재계산 3지점 (저장 / 삭제 / 모음집 이동) | §6.7, §6.9 | PR #1 — `CoverPolicy` + 불변식 자동 검증 |
| 🟡 | 길게 눌러 순서 재배치 (`sortIndex`) | §4 | PR #3 — 「전체 보기」의 `List.onMove`로 구현. **가로 캐러셀 드래그 재배치는 SwiftUI 기본 도구로 불가 — 후속 과제** |
| ✅ | `sortIndex` 중복 시 `createdAt` 타이브레이크 | §6.9 | PR #1 — `collections()` 2키 정렬. 화면의 `@Query`도 같은 2키를 쓴다 |
| ✅ | 「전체 보기」 그리드 | §2.2 | PR #3 — `CollectionGridView` (재배치도 여기) |
| ✅ | 모음집 상세 — 캔버스 그리드 (최신순/오래된순) | §2.2 | PR #3 — `CollectionDetailView` + 정렬 메뉴 |
| 🟡 | 초안 배너 "이어서 만들까요?" | §6.5 | PR #3 — `DraftBannerPolicy` 판정 + 배너 표시. **탭 동작은 에디터 진입(Phase 3)** |
| ⬜ | 통합 검색 (모음집 이름 + 캔버스 제목) | §10 | **v1.1로 이월** |
| ⬜ | 첫 실행 샘플 모음집 1개 생성 | §12 | 샘플 이미지 자산 확보 후 |

## P2

| 상태 | 항목 | 설계 참조 |
|------|------|-----------|
| ⬜ | 캔버스 다중 모음집 소속 | §19 |
| ⬜ | 모음집 표지 직접 지정 (사진 업로드) | §19 |
